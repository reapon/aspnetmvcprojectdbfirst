CREATE TABLE CustomerRegistraion
(
	CustomerRegistraionID INT NOT NULL PRIMARY KEY Identity,
	CustomerName varchar(50),
	Gender varchar(20),
	Email varchar(30),
	Phone varchar(20),
	Age int,
	Address varchar(100)
)
